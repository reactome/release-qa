# Configuration for running the SingleAttributeMultipleValueCheck to list
# instances having more than one value in the configured attributes.
# There is one line per class with tab-separated tokens. The first token
# is the class name. The remaining tokens are the attributes. Each attribute
# is checked separately.
Complex	compartment
