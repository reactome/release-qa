# Configuration for running the SingleAttributeMissingCheck to list
# instances not having a value in the configured attributes. There is
# one line per class with tab-separated tokens. The first token is the
# class name. The remaining tokens are the attributes. Each attribute
# is checked separately.
BlackBoxEvent	output	input
FailedReaction disease
CatalystActivity activity
LiteratureReference  pubMedIdentifier